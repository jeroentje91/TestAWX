---
- hosts: all
  tasks:
  - name: Windows eerst rebooten
    win_reboot:
  - name: Firefox installeren via MSI
    win_package:
      path: https://download.mozilla.org/?product=firefox-msi-latest-ssl&os=win64&lang=en-US
      product_id: Firefox  
      state: present
      user_name: Testuser
      user_password: Welkom01
  - name: alle updates installeren
    win_updates:
      category_names:
      - CriticalUpdates
      - SecurityUpdates
      state: installed
    register: updates
  - name: Reboot host if required
    win_reboot:
    when: updates.reboot_required
