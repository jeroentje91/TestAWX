---
- hosts: all
 #vars:
 #ansible_user: '{{pmp_accountname}}'
 #ansible_pass: '{{lookup('passwordmanagerpro','{{pmp_resourcename}}','{{pmp_accountname}}'
  tasks:
   - name: User aanmaken
     win_command: New-ADUser -Name “Jeroen Hofland" -Description "Gemaakt in Ansible" -path "OU=Test, DC=Hofland, DC=org"
       